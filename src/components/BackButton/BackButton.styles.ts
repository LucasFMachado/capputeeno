import { styled } from 'styled-components'

export const Button = styled.button`
  display: flex;
  gap: 8px;
  margin: 12px 0;
  font-weight: 500;
  font-size: 14px;
  line-height: 150%;
  color: var(--color-grey-700);

  svg {
    height: 20px;
    width: 20px;
  }
`
